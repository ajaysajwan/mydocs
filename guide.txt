This is a guide for initial setup of Git Repo

List of commsnds-
1. git config --list
2. git config --global user.name ""
3. git config --global user.email ""

4. git config --global credential.helper wincred

git init
git status
git add .
git commit -m "Adds/Updates/Re-factors"
git log
git show "check sum"

[Add+Commit]
git commit -a -m "Adds comments"
git push -u origin master
